{
    "name": "konux-interview",
    "version": "0.0.0",
    "engines": {
        "node": ">=16.4.2"
    },
    "scripts": {
        "uiTests": "npx cypress run --spec 'cypress/e2e/UI/**.js' --headed --browser chrome",
        "loginE2e": "npx cypress run --spec 'cypress/e2e/UI/login.cy'",
        "DB": "npx cypress run --spec 'cypress/e2e/DB/**",
        "e2e": "npx cypress run --spec 'cypress/e2e/e2eTests/**'",
        "apis": "npx cypress run --spec 'cypress/e2e/API/**'"
    },
    "dependencies": {
        "mysql": "^2.18.1"
    },
    "devDependencies": {
        "cypress-iframe": "^1.0.1"
    }
}
